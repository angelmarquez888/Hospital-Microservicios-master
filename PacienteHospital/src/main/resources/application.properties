# 1. NOMBRE Y PUERTO (Cambiar en cada microservicio)
spring.application.name=PacienteHospital
server.port=8088

# 2. CONEXIÓN A LA BASE DE DATOS
# Cambia "hospital_alertas" por la base de datos que le corresponda a la carpeta
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_paciente?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 3. CONFIGURACIÓN DE JPA / HIBERNATE
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# 4. CONECTARSE A EUREKA (El jefe)
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/